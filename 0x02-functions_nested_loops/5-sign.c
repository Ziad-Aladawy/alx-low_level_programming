#include "main.h"


/**
* print_sign - Function
* Description: it shows the sign of the number given
* @n: the number to be checked
* Return: returns the 1 if positive -1 if negative and 0 if 0
*/
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	if else (n < 0)
	{
		_putchar('-');
		return (1);
	}
	else
	{
		_putchar('0');
		return (1);
	}
}
